import { createContext } from "react";

export const CharacterInfoContext = createContext({});
export const ClassSpellsDetailsContext = createContext({});
export const AuthContext = createContext({});
export const FeatureTrackersContext = createContext({});
export const CharacterSessionContext = createContext({});
export const FeatureOverridesContext = createContext({});
export const UntrackedFeatureChoicesContext = createContext({});
export const SoulknifeCustomUsesContext = createContext({});
